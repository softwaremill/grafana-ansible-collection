---
- name: Zypper - Install Alloy from remote URL
  community.general.zypper:
    name: "{{ alloy_package }}"
    state: present
    disable_gpg_check: true
  notify: restart alloy
  when: __deploy_alloy | bool
